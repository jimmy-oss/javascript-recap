let age = 30;
age = 31;
console.log(age);

const birthYear = 1991;
console.log(birthYear);

// let is used if the declared value will change which
// is immutable as compared to const the value which has
// been declared cannot change according to the js world